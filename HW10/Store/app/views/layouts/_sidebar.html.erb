<div class="col-2 flex-shrink-1 w-auto p-4 bg-light">
  <div class="category">
    <h6>Categories:</h6>
  </div>
  <ul class="nav nav-pills flex-column mb-auto">
    <% Category.all.each do |category| %>
      <li class="nav-item">
        <%= link_to category.title, category %>
      </li>
    <% end %>
  </ul>
</div>
